{
  "name": "@meta-engine/shared",
  "version": "1.0.0",
  "description": "Shared utilities and types for meta-engine",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "lint": "echo 'No lint configured'"
  },
  "devDependencies": {
    "@types/lodash-es": "^4.17.12",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": ">=4.5.0"
  },
  "dependencies": {
    "lodash-es": "^4.17.23"
  }
}
