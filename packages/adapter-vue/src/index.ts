import { h } from "vue";

const adapterOptions = {
  // 渲染函数
  render: () => {
    // return createElement();
  },

  //   创建布局元素
  createLayoutElement: () => {
    return;
  },

  //   创建布局子元素
  createLayoutItemElement: () => {
    return;
  },
};
